# Architecture Decision Records

本目录记录 Portwise 中难以逆转、且没有上下文会令人意外的架构选择。

- `CONTEXT.md` 定义领域术语、产品范围和运行约束。
- `docs/architecture-design.md` 描述当前实现地图、依赖方向和验证方式。
- 本目录的 ADR 记录选择背后的权衡；实现发生变化时，先更新对应 ADR，再同步实现地图。

编号只递增，不复用已删除的编号。状态使用 `Accepted`、`Superseded` 或 `Rejected`。
