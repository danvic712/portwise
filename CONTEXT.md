# Portwise

Portwise（Dividend Harvest）是面向个人 A 股长期投资者的股息积累与分批交易参考工具。系统只提供可解释的分析和操作建议，不执行交易，也不保证收益。

## Scope and data

**私人工具**：只为单一用户服务的个人投资工具，不以公开多用户产品为当前目标。

**V1 组合上下文**：当前建账流程只允许建立一个投资组合；接口不要求调用方传递 `portfolio_id`，Application 通过唯一组合解析组合范围。未来支持多组合时必须显式扩展接口和数据隔离规则。

**A 股**：本项目首期唯一支持的股票市场范围；股票、行情、股息和交易数据都按 A 股代码独立记录。

**FTShare**：提供行情、财务、股息和历史数据的外部金融数据服务。本项目把它作为数据来源，不把它视为用户持仓或交易记录的归属地。

**股票资料**：围绕一只股票保存的识别信息、行情历史、财务/股息数据和数据来源信息。

## Hosting and runtime

**独立 Docker 部署**：前端和后端构建为同一个 Docker 镜像，由一个 ASP.NET Core 应用作为 HTTP 入口；不绑定 Cloudflare，也不依赖 Cloudflare Access 或其他 Cloudflare 专属运行时能力。

**ASP.NET Core Controllers**：后端使用 ASP.NET Core Controllers 提供 `/api` 接口，并由同一应用提供前端构建产物和 SPA fallback；`/healthz` 与 `/readyz` 使用 ASP.NET Core 原生 Health Checks。前端不直接连接 FTShare。

**FTShare 适配边界**：FTShare MCP 只在后端数据适配层使用，用于获取股票资料、历史行情和股息数据。访问凭据（如部署环境需要）只通过运行时安全配置注入，不能进入源码、镜像前端资源、日志或本地文档。

**容器持久化**：镜像本身保持可替换、无状态；用户持仓、交易记录和模型配置等私有数据必须通过容器外的持久化存储保存，不能只写入容器临时文件系统。

## Portfolio model

**持仓**：用户当前拥有的某只股票数量及其成本信息。

**交易记录**：用户对某只股票发生的买入或卖出历史，至少包含交易方向、数量、价格和日期，用于还原持仓与成本。

**持仓成本**：根据某只股票的交易记录计算出的当前持仓加权平均成本，不与外部行情数据混为一谈。

**期初持仓**：快速建账时用户确认的当前持股数量和持仓成本，作为后续交易记录的起点；它不伪装成完整的历史交易流水。

**核心仓**：用户计划长期持有、减仓时必须保留的最低持股数量。_Avoid_: 底仓、长期仓。

**卫星仓**：超出核心仓、可以用于阶段性波段操作的持股数量。_Avoid_: 可卖仓。

**目标股数**：用户为长期积累设定的规划数量，不是强制买入条件，也不是清仓条件。_Avoid_: 清仓目标。

## Dividend model

**模型股息**：用于计算股息率和价格区域的每股股息，可来自 TTM 实际股息、预计未来股息或用户自定义股息；它不等同于用户已经收到的股息。_Avoid_: 已得股息。

**TTM 实际股息**：最近连续十二个月内已经实施的每股股息合计。它不是实时变化的股息值；使用当前价格计算时，得到的是当前时点的 TTM 股息率。_Avoid_: 实时股息。

**TTM 股息率**：TTM 实际股息除以当前价格得到的当前参考比例，随价格或有效股息数据更新而变化。

**已实施股息**：已经实际发放或确认实施的股息。

**拟派股息**：已经公布但尚未实际发放的股息，不能计入用户已经获得的股息。

**已取消股息**：原计划已被取消的股息，不参与当前股息计算。

**股息率价格区域**：由模型股息和目标股息率计算出的价格参考范围，不是股价预测，也不是保证成交的目标价。

**数据快照**：某次从 FTShare 获取的、带有数据日期和来源标识的股票资料或行情结果。

**过期数据**：最近一次成功获取但数据日期早于当前交易日的数据。使用过期数据时必须明确展示其数据日期和来源。

**强买入区**：股价较低、模型股息率较高，可以增加投入力度的价格区域。

**分批加仓区**：适合按计划逐步买入的价格区域。

**持有区**：继续持有，不主动加仓或减仓的价格区域。

**减仓候选区**：股价偏高，可以考虑卖出部分卫星仓的价格区域。

**激进减仓区**：股价明显偏高，可以考虑卖出更多卫星仓，但仍必须保留核心仓的价格区域。

## Recommendation model

**操作建议**：系统根据股息率价格区域、持仓结构、资金计划和可选的 PB 辅助门槛生成的解释性结果。它不是买卖指令，系统不自动下单。

**建议股数**：根据操作区域、持仓、资金计划和交易单位计算出的参考操作数量，不代表必须成交的订单数量。

**模型参数模板**：一组可复制到多只股票的初始模型参数；每只股票可以单独覆盖模板值。_Avoid_: 全局固定规则。

**关注股票**：用户主动加入并希望持续查看、同步和分析的 A 股股票。

**计划再投资比例**：用户计划将可用股息收入重新投入股票的比例；它是长期规划参数，不代表已经收到或已经执行的股息再投资。

**模型不可用**：关键股息或模型参数缺失，系统无法可靠计算价格区域或操作建议时的状态。此时仍可展示可用的行情和持仓信息，但不能显示买入或减仓建议。

**谨慎参考**：股息率条件可计算，但 PB 辅助条件因数据缺失无法确认时的降级建议状态。

**价格阶梯**：按强买入、分批加仓、持有、减仓候选和激进减仓排列的价格区域可视化。它表达参考区间，不表达确定的未来价格路径。

**PB 辅助门槛**：用于增强或削弱股息率建议的可选条件，不能单独决定买入或卖出。

## Documentation authority

领域术语和产品范围以本文件为准；难以逆转的架构决策记录在 `docs/adr/`；当前实现、依赖方向和目录地图记录在 `docs/architecture-design.md`。如果本地 agent 笔记与代码或上述受版本控制的文档冲突，以代码和受版本控制的文档为准。
