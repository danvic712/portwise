name: Build and test

on:
  push:
    branches:
      - '**'
  pull_request:

permissions:
  contents: read

concurrency:
  group: build-test-${{ github.ref }}
  cancel-in-progress: true

env:
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages

jobs:
  build-test:
    name: Build and test
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 12.3.4

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '24.16.x'
          cache: pnpm
          cache-dependency-path: src/DividendHarvest.Web/pnpm-lock.yaml

      - name: Install frontend dependencies
        working-directory: src/DividendHarvest.Web
        run: pnpm install --frozen-lockfile

      - name: Build frontend
        working-directory: src/DividendHarvest.Web
        run: pnpm build

      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ${{ env.NUGET_PACKAGES }}
          key: nuget-${{ runner.os }}-${{ hashFiles('DividendHarvest.slnx', '**/*.csproj') }}
          restore-keys: |
            nuget-${{ runner.os }}-

      - name: Restore .NET dependencies
        run: dotnet restore DividendHarvest.slnx

      - name: Build solution
        run: dotnet build DividendHarvest.slnx --configuration Release --no-restore

      - name: Run tests
        run: dotnet test DividendHarvest.slnx --configuration Release --no-build --no-restore
