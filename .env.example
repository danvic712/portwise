# Local development only. Replace every value before using this setup outside a
# private development machine, and provide secrets through a secret manager.
POSTGRES_DB=portwise
POSTGRES_USER=portwise
POSTGRES_PASSWORD=portwise_dev_only
PORTWISE_HOST_PORT=8080
